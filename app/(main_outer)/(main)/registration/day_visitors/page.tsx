import DayVisitorRegistrationClient from "@/app/(main_outer)/(main)/registration/day_visitors/client";

export default async function DayVisitorRegistrationPage() {
  return <DayVisitorRegistrationClient/>
}